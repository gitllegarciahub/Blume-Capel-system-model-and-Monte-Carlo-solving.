Description of files provided:

Blume-Capel C.cpp and Blume-Capel.py are the montecarlo simulations in C++ and python respectively. The python version takes way too long to run so C++ version is preferred.

graficas BC.py makes the graphs with the data extracted from the simulation.

ising.py makes the comparison graph between our simulation (Blume-Capel) and the theoretical Ising model as a comparison.
